export default function Mapbar() {
    return (
        <div className="mapbar">
        <h2 className="mapbar-content">Please allow ~30 seconds for map markers to load. Thank you!</h2>
        </div>
    );
}